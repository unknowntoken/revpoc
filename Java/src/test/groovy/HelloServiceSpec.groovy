import spock.lang.Shared
import spock.lang.Specification

public class HelloServiceSpec extends Specification {

/*    @Shared
    def client = new RESTClient("localhost:7654")

    def 'should return 200 on GET requests'() {
        when: 'send request for image metadata'
        def response = client.get( path : 'hello' )

        then: 'server returns 200 on get requests'
        assert response.status == 200 : 'response code should be 200'
    }*/
}